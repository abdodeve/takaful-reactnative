// import { SET_MAIN_IMAGE, MainImageType, Actions } from "./types";

// const initialState = -1;

// export const MainImageReducer = (state = initialState, action: Actions) => {
//   switch (action.type) {
//     case SET_MAIN_IMAGE:
//       return action.mainImage;
//     default:
//       return state;
//   }
// };
